import React from "react";
import { Row } from "react-bootstrap";

const TopHeader = ({ color }) => {
  return <div style={{ color: color }} className="profile-text">Profile</div>;
};

export default TopHeader;
